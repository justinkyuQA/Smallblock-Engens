[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smallblock"
version = "0.1.0"
description = "SmallBlock Engine Framework"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["smallblock"]
